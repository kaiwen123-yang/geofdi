"""Baseline detectors for the comparison table (e07): GRU classifier (trained on FAULT data), window autoencoder
(nominal only), Mahalanobis gate (nominal only), and the unified FAR-calibration protocol shared with R-/R+."""
